{
  "name": "api",
  "version": "0.0.1",
  "description": "user service api",
  "main": "index.ts",
  "scripts": {
    "dev": "nodemon ./src/index.ts --swc",
    "node": "node -r ts-node/register ./src/index.ts --esm",
    "build": "tsc && cp -R package*.json build && cd build && npm ci --omit=dev",
    "test": "test"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/aws-serverless-express": "^3.3.5",
    "aws-serverless-express": "^3.4.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1"
  }
}
